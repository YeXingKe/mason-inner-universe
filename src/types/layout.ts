export type LayoutMode = 'default' | 'streamline'

export const layoutModeOptions: { label: string; value: LayoutMode }[] = [
  { label: '默认布局', value: 'default' },
  { label: '单栏布局', value: 'streamline' },
]
